# Hyprland Colors Configuration
# Auto-generated by Matugen

# Material You Color Scheme
$background = rgb({{colors.background.default.hex}})
$foreground = rgb({{colors.foreground.default.hex}})
$surface = rgb({{colors.surface.default.hex}})
$surface_bright = rgb({{colors.surface_bright.default.hex}})
$surface_dim = rgb({{colors.surface_dim.default.hex}})

# Primary colors
$primary = rgb({{colors.primary.default.hex}})
$primary_bright = rgb({{colors.primary_bright.default.hex}})
$primary_dim = rgb({{colors.primary_dim.default.hex}})

# Secondary colors  
$secondary = rgb({{colors.secondary.default.hex}})
$secondary_bright = rgb({{colors.secondary_bright.default.hex}})
$secondary_dim = rgb({{colors.secondary_dim.default.hex}})

# Tertiary colors
$tertiary = rgb({{colors.tertiary.default.hex}})
$tertiary_bright = rgb({{colors.tertiary_bright.default.hex}})
$tertiary_dim = rgb({{colors.tertiary_dim.default.hex}})

# Error colors
$error = rgb({{colors.error.default.hex}})
$error_bright = rgb({{colors.error_bright.default.hex}})
$error_dim = rgb({{colors.error_dim.default.hex}})

# Outline colors
$outline = rgb({{colors.outline.default.hex}})
$outline_variant = rgb({{colors.outline_variant.default.hex}})

# Shadow colors
$shadow = rgb({{colors.shadow.default.hex}})
$scrim = rgb({{colors.scrim.default.hex}})

# Inverse colors
$inverse_surface = rgb({{colors.inverse_surface.default.hex}})
$inverse_on_surface = rgb({{colors.inverse_on_surface.default.hex}})
$inverse_primary = rgb({{colors.inverse_primary.default.hex}})

# Border configurations
general {
    col.active_border = $primary $secondary 45deg
    col.inactive_border = rgba({{colors.outline.default.hex}}aa)
}

# Window decoration
decoration {
    col.shadow = rgba({{colors.shadow.default.hex}}66)
    col.shadow_inactive = rgba({{colors.shadow.default.hex}}22)
}

# Group colors
group {
    col.border_active = $primary
    col.border_inactive = $outline
    col.border_locked_active = $error
    col.border_locked_inactive = $error_dim
    
    groupbar {
        col.active = $primary
        col.inactive = $surface
        col.locked_active = $error
        col.locked_inactive = $error_dim
    }
}

# Misc colors for various UI elements
misc {
    # Background color for the hyprland logo
    col.splash = $primary
    # Background color for the lock screen
    background_color = $background
}
